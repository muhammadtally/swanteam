import styled from 'styled-components';

export const Button = styled.div`
   position: fixed; 
   width: 100%;
   left: 1%;
   bottom: 5vh;
   height: 3vh;
   font-size: 2rem;
   z-index: 1;
   cursor: pointer;
   color: #0e86e8;
`